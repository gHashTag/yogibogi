import { Dimensions } from 'react-native'

const BLUE = '#30d0fe'
const WHITE = '#fff'
const LIGHT_GRAY = '#D3D3D3'

const win = Dimensions.get('window')
const W = win.width
const H = win.height

export {
  W,
  H,
  BLUE,
  WHITE,
  LIGHT_GRAY
}
